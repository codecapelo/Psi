import { ModuleStub } from "@/components/ModuleStub";

export default function AuditoriaPdfStep() {
  return (
    <ModuleStub
      title="Auditoria de PDFs (IA)"
      description="Upload de documentos externos para extração e análise por IA."
    />
  );
}
