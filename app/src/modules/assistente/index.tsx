import { ModuleStub } from "@/components/ModuleStub";

export default function AssistenteStep() {
  return (
    <ModuleStub
      title="Psi Assistente (IA)"
      description="Auditor clínico: chat para discutir casos, validar hipóteses e referências."
    />
  );
}
