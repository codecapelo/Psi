import { ModuleStub } from "@/components/ModuleStub";

export default function FenomenologiaStep() {
  return (
    <ModuleStub
      title="Fenomenologia"
      description="Roteiro de Exame Fenomenológico — abordagem clínica (não diagnóstica)."
    />
  );
}
